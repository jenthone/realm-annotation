apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    compile 'com.squareup:javapoet:1.8.0'
    compile 'com.google.guava:guava:21.0'

    compile project(':realm-annotation-api')
}
